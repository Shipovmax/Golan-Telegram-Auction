# Минимальные зависимости для веб-приложения аукциона
# Установка: pip install -r requirements.txt

# Основной веб-фреймворк
Flask>=2.0.0

# Для работы с датами и временем (встроено в Python 3.7+)
# datetime уже встроен в Python

# Для работы с JSON (встроено в Python)
# json уже встроен в Python

# Для работы с типами данных (встроено в Python 3.5+)
# typing уже встроен в Python
